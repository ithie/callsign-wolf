import { mount, show } from './campaign-end-screen';

export const CallsignWolf = () => {
    mount(() => {});
    show('CALLSIGN WOLF');
};

export const KeineKampagnenName = () => {
    mount(() => {});
    show('');
};
