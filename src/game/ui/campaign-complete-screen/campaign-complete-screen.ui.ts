import { mount, show } from './campaign-complete-screen';

export const CallsignWolf = () => {
    mount(() => {});
    show('CALLSIGN WOLF');
};

export const KeineKampagnenName = () => {
    mount(() => {});
    show('');
};
