import { mount, show } from './imprint-page';

export const MitBackButton = () => {
    mount();
    show('https://ithie.github.io/callsign-wolf');
};

export const OhneBackButton = () => {
    mount();
    show();
};
